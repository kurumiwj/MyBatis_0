<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kurumi.mybatis.mapper.SelectMapper">
    <select id="getUserById" resultType="User">
        select * from t_user where id=#{id}
    </select>
    <select id="getAllUser" resultType="User">
        select * from t_user
    </select>
    <select id="getTotalUser" resultType="Integer">
        select count(*) from t_user
    </select>
    <select id="getUserById1" resultType="Map">
        select * from t_user where id=#{id}
    </select>
    <select id="getAllUser1" resultType="map">
        select * from t_user
    </select>
    <select id="getAllUser2" resultType="map">
        select * from t_user
    </select>
</mapper>